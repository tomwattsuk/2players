{
    "name": "2players",
    "version": "1.0.0",
    "scripts": {
        "start": "cd client && npm start",
        "build": "cd client && npm run build",
        "test": "cd client && npm test && cd ../server && npm test"
    },
    "dependencies": {
        "dotenv": "^16.4.5",
        "mongodb": "^6.8.0"
    }
}
